---
title: ""
pagetitle: "Advanced Lab 2 — Risk-Set Case-Control Analysis | LIPPS"
description: "Conditional logistic regression and incidence-density sampling."
page-layout: full
---
::: {.lipps-course-hero}
::: {.lipps-eyebrow}
EPIDEMIOLOGY II · LAB 02
:::
# Risk-set sampling and matched analysis
## Controls represent person-time at risk, not people destined to stay disease-free.
:::
::: {.lipps-section}
Download [riskset_case_control.csv](../data/riskset_case_control.csv). Each risk set contains one case and three controls sampled at the case event time.

### Tasks
1. Verify that each risk set has exactly one case.
2. Compare exposure prevalence in cases and controls.
3. Fit conditional logistic regression with `strata(risk_set)`.
4. Interpret the exposure OR as an incidence-rate ratio under incidence-density sampling.
5. Fit an inappropriate ordinary logistic regression ignoring risk sets and compare estimates/SEs.
6. Explain why future cases can validly appear as controls in an incidence-density design.
7. Describe how the target measure would differ under cumulative-incidence control sampling.

```r
library(survival)
d <- read.csv("riskset_case_control.csv")
fit <- clogit(case ~ exposed + age + strata(risk_set), data=d)
summary(fit)
```

### Methods defence
Your write-up must include one paragraph explaining the source population and why the sampling scheme—not disease rarity—is what permits rate-ratio interpretation.
:::
::: {.lipps-section}
## LAB PROTOCOL · Graduate submission standard

### Epidemiological question
How do risk-set sampling and matched analysis recover a rate-ratio estimand from sampled controls?

### Before opening R
Write down the target population, exposure/treatment contrast, outcome, time horizon and effect measure. State the design and the assumptions that would make the proposed estimate meaningful. This paragraph is part of the submission: code without an estimand is incomplete.

### Required analysis products
Reconstruct matched/risk sets, fit conditional logistic regression, compare with an inappropriate unmatched analysis, and explain the difference in estimand/standard errors.

Every numerical result should be accompanied by a plain-language interpretation. Avoid reporting software output as though coefficients interpret themselves. If a model is used, state the link function, covariates, rationale for adjustment and how uncertainty was calculated.

### Diagnostics that must appear in the report
Check set sizes, exposure variation within sets, late cases previously sampled as controls and whether matching factors were handled correctly.

A diagnostic is not a ceremonial plot. State what failure it is intended to reveal and what you would change if that failure were present.

### Reproducibility requirements
Your submission should run from a clean R session. Use relative paths, set no user-specific working directory, keep data cleaning separate from analysis, label derived variables, and generate the final tables/figures from code. Include `sessionInfo()` or an equivalent dependency record. Do not manually copy numbers from the console into the final table.

### Interpretation discipline
End with four statements:
1. **Estimate:** what was estimated, on what scale and with what uncertainty?
2. **Assumptions:** which design/identification assumptions matter most?
3. **Diagnostics:** what evidence supports or challenges those assumptions?
4. **Claim:** what can be concluded, and what stronger claim would not be justified?

### PhD extension
Create a weighted case-cohort-style analysis or simulate alternative control-sampling fractions and show which features of the effect estimate are preserved.

### Suggested marking rubric (20 marks)
- estimand/design logic — 4
- reproducible data management and code — 3
- correct analysis — 5
- diagnostics/sensitivity analysis — 4
- interpretation and limits of inference — 4

A technically correct model with an incoherent causal question should not receive a high distinction.
:::

